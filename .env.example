# Copy this file to .env.local and fill in real values.
# .env.local is gitignored — never commit real keys.
# Exact variable names below are placeholders until the matching
# approach (keyword vs. meaning-based) and provider are finalized.

# Example: matching/embedding service key, once one is chosen
# MATCHING_SERVICE_API_KEY=

# Example: if a live ranking/explaining LLM call is still used somewhere
# GROQ_API_KEY=
